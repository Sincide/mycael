# ~/.config/quickshell/mycael/.env.example
# Environment overrides for the Mycael shell. Copy to .env and edit as needed.
# CAELESTIA_BD_PATH points to the optional beat detector binary.
CAELESTIA_BD_PATH="$HOME/.local/bin/beat_detector"
